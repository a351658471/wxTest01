<!--components/leftTab/leftTab.wxml-->
  <view class='leftTab'>
     <block wx:for="{{tabList}}">
      <view style='font-size:{{fontSize}}' class='tabItem {{tabCurrent == index? "active" : ""}}' bindtap='tabClick' data-index='{{index}}'>{{item}}</view>
     </block>
  </view>